# ТЗ разработчику: интро-перелинковка «контент → услуги» (3 сайта)

**Дата:** 21–22 сентября 2026 (финал 22.09: только интро-ссылки)
**Сайты (WordPress + Bricks):**
1. decorativearchitecturalproducts.com (DP) — 11 постов, 12 кейсов
2. royalfoam.art (ART) — 5 постов, 24 кейса
3. decorativearchitecturalshapes.com (DS) — 9 постов, 12 кейсов

**Проблема (из аудитов):** тематическая перелинковка отсутствует — DP 0/23 пар, ART 0/29, DS 1/21. Контент лежит в JSON Bricks-билдера (`_bricks_page_content_2`), поэтому SQL-замены и обычные SEO-плагины автолинковки **запрещены** — работаем только по этой инструкции.

**Цель:** внедрить 65 интро-ссылок по `interlink-scheme-c.csv` (ровно 1 контекстная ссылка в интро каждой статьи/кейса, exact-match анкор) + точечные фиксы из п. 6.3. Больше никаких ссылок не ставить.

## 1. Состав пакета (всё нужное — в `interlink-package.zip`)

| Файл | Что это |
|---|---|
| `interlink-scheme-c.csv` | **Исполнительная таблица: 65 строк, все `body-intro`.** Одна строка = одна ссылка: откуда (`source_url`) → куда (`target_url`) → анкор (`anchor`). Это главный документ. |
| `interlink-scheme.csv` | Архив v1 — бэклог, **не исполнять** |
| `interlink-scheme.md` | Правила, объёмы, волны, страницы без услуги |
| `interlink-audit-products/art/shapes.md` + `.csv` (6 файлов) | Исходные аудиты: какие пары проверены, попутные находки и баги |
| `interlink-snippet-dp.php` / `-art.php` / `-ds.php` | **Готовые WPCode-сниппеты** (по одному на сайт, 16/29/20 правил, все mode=replace). Покрывают все 65 ссылок |
| `interlink-snippet-template.php` | Общий шаблон сниппета (для понимания кода) |
| `interlink-map.json` | Машиночитаемый маппинг всех 65 правил сниппета |
| `build-interlink-map.py` | Генератор маппинга и сниппетов из CSV: `python3 build-interlink-map.py interlink-scheme-c.csv` |
| `interlink-developer-brief.md` | Это ТЗ |

## 2. Что нужно от заказчика до старта

- [ ] Доступ Administrator в wp-admin × 3 сайта (или SFTP × 3 как альтернатива для mu-плагина)
- [ ] Staging-копии × 3 сайта (минимум — свежие бэкапы)
- [ ] PHP 7.4+ и включённое расширение `dom` на всех окружениях (проверить до старта)

## 3. Этап 1 — Сниппет на staging (× 3 сайта, ~1 ч на сайт)

На **staging**, по одному сайту за раз (порядок: DS → ART → DP, от меньшего к большему):

1. Plugins → установить **WPCode** (free достаточно) → Add Snippet → PHP → вставить **весь** файл своего сайта (`interlink-snippet-<site>.php`) → Location: **Run Everywhere** → Save (WPCode проверит синтаксис — сохранять только при отсутствии ошибок) → Activate.
2. Открыть новый пункт меню **Interlink Report**, сверить счётчик правил: DP = 16, ART = 29, DS = 20.
3. В инкогнито (мимо кэша) открыть все URL из отчёта. Режим по умолчанию **DRY-RUN**: вывод не меняется, каждое срабатывание пишется в `wp-content/uploads/interlink-dryrun.log`.
4. Разобрать лог (хвост виден прямо на странице отчёта):
   - `REPLACE … => FOUND` — фраза на месте, ссылка встанет автоматически;
   - `REPLACE … => NOT-FOUND` — **очередь ручной доработки**: либо скорректировать фразу в массиве правил (минимально, сохраняя смысл анкора из CSV), либо проставить ссылку вручную в Bricks-редакторе и удалить правило. **Ожидается <10% таких.**
   - Колонка `hook:` показывает, какой путь сработал (`bricks(content)` — основной; `the_content` — fallback). Если везде fallback — сообщить заказчику до go-live.
5. Зафиксировать итог по сайту: «N FOUND / M NOT-FOUND, ручные правки: список URL».

## 4. Этап 2 — QA staging (~1 ч на сайт)

- [ ] View-source 3 страниц (2 поста + 1 кейс): маркеры `<!--il-->` на месте, каждый — ровно 1 на страницу (видны только в LIVE; для проверки временно `INTERLINK_DRY_RUN=false`, затем вернуть `true`)
- [ ] Ссылки не внутри других ссылок/кнопок, вёрстка не поплыла (мобильная + десктоп)
- [ ] Ни одна ссылка не ведёт на 404
- [ ] Страница Interlink Report открывается, лог пишется
- [ ] PageSpeed до/после — без значимой деградации (со включённым page-cache разница должна быть ~нулевой)

## 5. Этап 3 — Go-live сниппета (× 3 сайта)

1. Свежий бэкап продакшена.
2. Повторить установку из Этапа 1 на проде (финальная версия файла со staging, включая правки NOT-FOUND).
3. `INTERLINK_DRY_RUN` → `false` → **очистить весь кэш** (плагин кэша + CDN/Cloudflare) → проверить 3 страницы из Этапа 2.
4. **Откат в любой момент:** деактивировать сниппет в WPCode — вывод мгновенно вернётся к исходному (база не менялась).

## 6. Ручные работы (не покрыты сниппетом)

### 6.1. ART-007: удалить ошибочную ссылку (1 правка)

В посте №4 (`…/custom-architectural-foam-elements…`) удалить существующую ссылку середины текста на `foam-monument-signs`. Интро-ссылка на `architectural-shapes` встанет автоматически по строке ART-007. Итог на странице: ровно 1 тематическая ссылка.

### 6.2. Исключено решением заказчика (не делать)

CTA-кнопки кейсов, вторые ссылки из финальных блоков, Related-блоки «услуги → контент», линковка списка «Related Products», возврат 2 выпавших DS-кейсов в ротацию Similar. Всё это — в бэклоге (v1 CSV + история git).

### 6.3. Фиксы шаблонов «волна 0» (не добавляют ссылок, до всего остального на проде)

- DP: кнопка «See all services» в блоке «What we do» → вести на `/services/` (сейчас ведёт на `/blog`).
- DS: убрать self-link из «Similar projects» (исключить текущую страницу из выборки).
- ART: статичный блок «3 oldest posts» — оставить как есть или переключить на «3 latest» (тематизация — бэклог).

### 6.4. Контентные правки (dev — только если это шаблон; иначе контент-менеджер)

- ART: FAQ кейса «1 Billion & Beyond» — чужой бренд «Decorative Architectural Shapes» → «Royal Foam».
- DS: карточка shutters с чужим анонсом; текст Jacksonville (дубль Naples); опечатки «Geren Wall», «Culumn», «Belley»; slug поста keystones (с 301-редиректом).
- DP: 404-пост про porch columns — восстановить или редирект (решение заказчика).
- DS-пост про тримы (`how-to-choose-exterior-polyurethane-trim-for-your-home`) уже содержит 1 контекстную ссылку — **не трогать**, строки в схеме нет осознанно.

## 7. Финальная приёмка (критерии закрытия задачи)

- [ ] Все 65 строк из `interlink-scheme-c.csv` внедрены (проверка: полный проход по Interlink Report + выборка по 3 URL на сайт)
- [ ] В логе сниппета нет NOT-FOUND (или есть список согласованных ручных исключений с проставленными вручную ссылками)
- [ ] На каждой странице контента — ровно 1 новая тематическая ссылка в интро (кроме DS-поста про тримы: там 1 существующая, без изменений)
- [ ] Фиксы п. 6.3 применены на проде; ошибочная ссылка ART-007 удалена
- [ ] Кэш очищен, мобильная вёрстка проверена, PageSpeed без деградации

## 8. Ограничения и запреты

- **Не делать SQL REPLACE по базе** (сломает сериализованный Bricks-JSON).
- **Не ставить плагины автоперелинковки** (Link Whisper и аналоги) поверх этого решения — дубли и конфликты.
- Не менять анкоры без согласования (exact-match — часть схемы).
- Не исполнять строки из бэклога (архивная `interlink-scheme.csv`, Related-блоки, CTA).
- Все правки сначала на staging, на прод — только проверенное.

## 9. Ориентировочная трудоёмкость

| Работа | Оценка |
|---|---|
| Этапы 1–3 (сниппет: staging → QA → прод) × 3 сайта | 2–4 ч |
| Фиксы п. 6.3 + ART-007 | 1–2 ч |
| Приёмка + запас на NOT-FOUND | 1 ч |
| **Итого** | **4–7 ч** |

---
*Вопросы по схеме — к заказчику с указанием ID строк из `interlink-scheme-c.csv` (формат: `ART-033: …`).*
